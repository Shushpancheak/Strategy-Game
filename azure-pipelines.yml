trigger:
- dev
- master

pool:
  vmImage: 'ubuntu-latest'

container: ubuntu:19.10

steps:
- script: |
    docker exec -u 0
    sudo apt-get install libsfml-dev
    mkdir build
    cd build
    cmake ..
    make
