language: cpp
compiler: gcc
dist: xenial

before_install:
  - sudo apt-get update -y
  - sudo apt-get install libsfml-dev=2.5.1+dfsg-1 -y

script:
  - mkdir build
  - cd build
  - cmake ..
  - make

notifications:
  email: makcieml@gmail.com
