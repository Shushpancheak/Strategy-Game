language: cpp
compiler:
- gcc
os:
- linux
addons:
  apt:
    packages:
      - libsfml-dev

script:
  - sudo apt-get install libsfml-dev
  - mkdir build
  - cd build
  - cmake ..
  - make

notifications:
  email: makcieml@gmail.com
