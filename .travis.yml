language: cpp
compiler:
- gcc
os:
- linux
addons:
  apt:
    update: true
    packages:
      - libsfml-dev 2.5

script:
  - mkdir build
  - cd build
  - cmake ..
  - make

notifications:
  email: makcieml@gmail.com
